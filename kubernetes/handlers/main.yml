  - name: reload systemd
    ansible.builtin.systemd:
      daemon_reload: true

  - name: restart containerd
    ansible.builtin.systemd:
      name: containerd.service
      state: restarted
      enabled: true

